# 과제 8 제출문 — 내 소개 페이지에 패스키 달기

> 아래 `[실기 입력]` 표시만 실제 배포 뒤 본인 기기의 요청/응답 결과로 채웁니다. 소스 구현 자체는 이 프로젝트에 포함되어 있습니다.

## 공개 결과물 주소

`[실기 입력: HTTPS 결과물 URL]`

## 소스 주소

`[실기 입력: 공개 GitHub HTTPS URL]`

소개 첫 화면은 공개이고, `PRIVATE` 영역만 패스키 인증 뒤 서버에서 자료를 받아 표시한다. 기존 공개 소개 내용과 내가 만든 웹 프로젝트 4개 링크는 그대로 공개되어 있다.

---

# 인증 구현 설명서 — 여섯 항목

## ① 무엇으로 붙였나

외부 인증 서비스나 WebAuthn 서버 라이브러리 없이 **직접 구현한 Node.js 서버**에 WebAuthn 패스키 인증을 붙였다. 서버는 Node.js 표준 모듈 `http`, `crypto`, `fs`만 사용하고, 브라우저에서는 `navigator.credentials.create()`와 `navigator.credentials.get()`을 직접 호출한다. 비밀번호 인증은 만들지 않았다.

등록 때 서버가 새 challenge를 만들고 브라우저가 패스키를 생성한다. 등록 응답을 검증한 뒤 서버에는 credential ID, **공개키**, counter, transports, 패스키 이름, 등록 날짜 같은 정보만 저장한다. 개인키를 받거나 저장하는 코드와 비밀번호 컬럼은 없다.

로그인 때도 새 challenge를 발급한다. 기기가 개인키로 서명한 응답을 보내면 서버가 등록 때 저장한 공개키로 서명을 검증하고, 성공한 경우에만 HttpOnly 세션 쿠키를 만든다.

## ② 왜 그걸 골랐나

이번 과제의 핵심인 “비밀번호 없이 공개키 + 일회용 challenge + 서명 검증” 흐름을 직접 확인하기 위해 WebAuthn을 사용했다. 인증 서비스를 통째로 맡기는 대신 브라우저와 서버 사이에서 등록 options, verify, 로그인 options, verify가 어디를 지나는지 소스에서 바로 볼 수 있도록 구성했다.

서버에서 challenge·origin·RP ID·사용자 확인 플래그를 직접 검사하고, 등록 응답의 COSE 공개키를 파싱해 JWK 형태로 저장한다. 로그인에서는 저장 공개키로 assertion 서명을 직접 검증하고 sign counter를 확인한다. 세션과 비공개 자료 권한 확인도 직접 작성했다.

## ③ 어디를 어떻게 고쳤나

- **화면 / 공개·비공개 경계** — `public/index.html`
  - 기존 소개 페이지와 웹 프로젝트 4개는 공개로 유지했다.
  - `#private` 아래에 패스키 잠금 영역, 로그인 뒤 자료 화면, 패스키 목록을 추가했고 제출용 확인 기능은 접힌 `과제 검증 도구` 안에 두었다.
  - 공개 HTML에는 실제 비공개 항목 제목/본문을 넣지 않았다.

- **브라우저 등록** — `public/app.js`의 `registerPasskey()`
  - `/api/register/options`에서 challenge를 받고 `navigator.credentials.create()`를 호출한다.
  - 브라우저 반환값을 직렬화해 `/api/register/verify`로 전송한다.
  - 등록 창을 취소하면 `/api/register/cancel`을 호출해 미사용 challenge를 지운다.

- **서버 등록** — `server.js`의 `/api/register/options`, `/api/register/verify`
  - `crypto.randomBytes()`로 매번 다른 challenge를 만든다.
  - challenge를 서버 JSON 저장소에 보관하고 verify 요청에서 한 번 꺼내면서 즉시 삭제한다.
  - `clientDataJSON`, origin, RP ID hash, user presence/user verification flag를 검사한다.
  - authenticator data에서 credential ID와 COSE 공개키를 파싱하고 검증이 성공한 경우에만 공개키를 저장한다.

- **브라우저 로그인** — `public/app.js`의 `getAssertion()`, `login()`
  - `/api/login/options`에서 새 challenge를 받고 `navigator.credentials.get()`으로 서명 응답을 만든다.
  - `/api/login/verify`로 보내 검증 결과를 받는다.

- **서버 로그인** — `server.js`의 `/api/login/options`, `/api/login/verify`
  - 매 요청마다 새 challenge를 발급하고 서버에 보관한다.
  - verify 시 challenge를 먼저 소비하기 때문에 같은 응답을 재전송해도 다시 쓸 수 없다.
  - 등록된 credential ID와 저장 공개키를 찾는다.
  - `authenticatorData + SHA-256(clientDataJSON)` 서명 데이터를 만들고 Node.js `crypto.verify()`로 ECDSA/RSA 서명을 검증한다.
  - 성공하면 HttpOnly 세션 쿠키를 만든다. 제출용 화면에는 세션 원문을 표시하지 않는다.

- **로그아웃** — `server.js`의 `POST /api/logout`
  - 서버 세션을 삭제하고 쿠키를 만료시킨다.

- **비공개 자료 조회** — `server.js`의 `GET /api/private`
  - 로그인 세션이 없으면 401이다.
  - URL의 `userId`를 신뢰하지 않고 서버가 확인한 HttpOnly 세션의 `userId`로만 자료를 고른다.
  - `GET /api/private/users/:userId`에서 다른 계정을 지정하면 403이다.

- **패스키 삭제** — `server.js`의 `DELETE /api/passkeys/:id`
  - 현재 세션 계정의 패스키만 삭제할 수 있다.
  - 하나만 남은 경우 마지막 패스키 삭제를 409로 막는다.

## ④ 안 열리는 것을 확인한 기록

실제 배포 후 `PRIVATE` 영역의 `과제 검증 도구 보기`와 브라우저 Network 기록을 사용한다.

### A. 로그인 없이 열기

성공 요청: 패스키 로그인 후 `GET /api/private` → `[실기 입력: 200]`

거절 요청: 쿠키 없이 `GET /api/private` → `[실기 입력: 401]`

### B. 남의 계정 자료 열기

소유자 세션 → `GET /api/private/users/tester` → `[실기 입력: 403]`

테스트 세션 → `GET /api/private/users/owner` → `[실기 입력: 403]`

거절 응답에는 대상 자료 건수의 `countBefore`, `countAfter`가 들어가며 읽기 거절 전후가 같은지 기록한다.

### C. 이미 쓴 challenge 재사용

정상 로그인 → `[실기 입력: 200]`

같은 `challengeId + authentication response` 재전송 → `[실기 입력: 401 CHALLENGE_MISSING_OR_USED]`

서버는 verify 전에 저장 challenge를 한 번 꺼내면서 삭제하므로 한 번 사용한 로그인 응답을 다시 검증하지 않는다.

### D. 패스키 삭제 뒤 로그인

패스키 2개 등록 → 패스키 A를 서버 목록에서 삭제 → 패스키 B 로그인 → `[실기 입력: 200]`

삭제한 credential A를 사용한 로그인 → `[실기 입력: 401 / 등록 credential 불일치]`

### E. 틀린 서명

정상 패스키로 새 authentication assertion을 만든 뒤 브라우저 테스트 버튼이 signature 문자열을 일부러 한 글자 바꾸어 보낸다.

정상 서명 요청 → `[실기 입력: 200]`

변조 서명 요청 → `[실기 입력: 401 AUTHENTICATION_VERIFICATION_FAILED]`

## ⑤ AI와 나

AI에게 맡긴 일: 기존 HTML 구조를 분석하고 WebAuthn 등록·로그인 API, 패스키 관리 UI, 과제 확인용 요청/응답 기록 코드와 제출문 초안을 만드는 일을 맡겼다.

내가 직접 판단한 일: 기존 소개 내용과 내가 만든 웹 프로젝트는 계속 공개하고, 새로 만든 가상 메모 3개만 PRIVATE 영역에서 서버 인증 뒤 내려받도록 정했다. 실제 개인정보는 넣지 않기로 판단했다.

AI 말을 따르지 않은 일: 실제 패스키 생성·생체/PIN 확인·두 번째 기기 등록은 AI가 대신할 수 없으므로 자동 완료로 처리하지 않고, 내 기기에서 직접 실행한 결과만 최종 증거로 제출한다.

## ⑥ 아직 못 막은 것

**새 배포 직후 최초 소유자 패스키가 0개인 짧은 구간에는 최초 등록 endpoint가 공개되어 있다.** 과제에서 비밀번호나 별도 로그인 수단을 만들지 않기 위해 이렇게 두었고, 본인이 배포 직후 첫 패스키를 바로 등록하면 이후에는 추가 등록이 기존 패스키 세션을 요구한다. 실제 서비스라면 최초 설정을 사내망/IP 제한, 일회용 배포 절차, 관리자 콘솔 등 별도 안전한 부트스트랩 절차로 막아야 한다.

또한 현재 데이터 저장은 단일 Node 프로세스의 JSON 파일이다. 과제용으로 동작하지만 여러 서버 인스턴스가 동시에 쓰는 실제 서비스에서는 PostgreSQL 같은 트랜잭션 DB와 중앙 세션 저장소로 바꿔야 한다.

---

# 짧은 확인 방법 4줄

**어디로 가나요:** 결과물 첫 화면을 연 뒤 상단 `PRIVATE` 또는 페이지 아래 `PASSKEY PRIVATE AREA`로 간다.

**세 단계 안에 무엇을 하나요:** ① 패스키로 로그인한다 → ② 비공개 자료 3개를 확인한다 → ③ `과제 검증 도구 보기`에서 401/403/재사용/서명 변조 테스트를 누른다.

**무엇이 보이면 통과인가요:** 로그인 뒤 가상 비공개 자료 3개와 패스키 목록이 보이고, 정상 요청은 200, 로그인 없는 요청/재사용/틀린 서명은 401, 다른 계정 자료는 403으로 기록되면 된다.

**안 될 때는 무엇이 보이나요:** HTTPS가 아니면 WebAuthn 경고가 나오고, 인증 실패/취소/마지막 패스키 삭제 시 각각 오류 안내와 HTTP 상태 코드가 확인 기록에 남는다.

---

# AI와 내 판단 3줄

**AI에게 맡긴 일:** HTML 연동, WebAuthn 서버/브라우저 코드, 테스트 기능, 제출문 구조 초안.

**내가 직접 판단한 일:** 공개/비공개 자료 범위, 가상 데이터 사용, 실제 기기에 어떤 패스키 두 개를 등록할지와 최종 증거 선택.

**AI 말을 따르지 않은 일:** 실제 기기에서 일어나야 하는 패스키 등록/생체 인증 결과를 AI가 완료했다고 간주하지 않았다. 실제 요청/응답만 제출하기 위해서다.

---

# 완주 체크리스트

- [x] 공개 영역과 PRIVATE 영역을 화면에서 분리했다.
- [ ] 배포 도메인에서 첫 패스키를 실제 등록했다.
- [ ] 서버 패스키 목록에서 저장된 공개키를 캡처했다.
- [ ] 등록 요청 본문에 개인키가 없음을 Network 기록으로 캡처했다.
- [ ] 등록 challenge 두 개가 서로 다름을 audit/Network로 캡처했다.
- [ ] 로그인 challenge 두 개가 서로 다름을 audit/Network로 캡처했다.
- [ ] 정상 서명 로그인 성공과 변조 서명 401을 나란히 캡처했다.
- [ ] 이미 사용한 로그인 응답 재사용 401을 캡처했다.
- [x] 비밀번호 입력칸이 소스에 없다.
- [x] 로그인하지 않으면 `/api/private`가 401을 반환하도록 구현했다.
- [x] 공개 `index.html`에 서버 비공개 항목 제목/본문을 넣지 않았다.
- [ ] 실제 배포에서 로그인 없는 응답 본문/페이지 소스를 캡처했다.
- [ ] 소유자와 테스트 계정에 각각 패스키를 실제 등록했다.
- [ ] 양방향 다른 계정 자료 요청 403을 캡처했다.
- [ ] 거절 전후 자료 건수가 같음을 기록했다.
- [ ] 다른 `userId`를 보내도 세션 사용자의 자료만 반환됨을 캡처했다.
- [ ] 한 계정에 패스키 두 개를 실제 등록했다.
- [ ] 하나를 삭제한 뒤 남은 패스키 로그인 성공을 캡처했다.
- [ ] 삭제한 패스키가 서버에서 더 이상 허용되지 않음을 기록했다.
- [x] 마지막 패스키 하나는 409로 삭제를 막도록 구현했다.
- [x] 실제 개인정보/비밀값 대신 가상 비공개 자료만 사용했다.
